<Page
    x:Class="PdfViewer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PdfViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"></RowDefinition>
			<RowDefinition Height="50"></RowDefinition>
		</Grid.RowDefinitions>

		<ScrollViewer ZoomMode="Enabled" Background="DarkGray" Grid.Row="0">
			<ItemsControl ItemsSource="{x:Bind PdfPages}">
				<ItemsControl.ItemTemplate>
					<DataTemplate x:DataType="BitmapImage">
						<Image Source="{x:Bind}" Margin="0 2" />
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</ScrollViewer>

		<Image Name="PdfImage" Grid.Row="0" Margin="10"></Image>
		<Button Grid.Row="1" Margin="10,0,0,0" Click="Button_Click_Open_PDF" >Open</Button>
		<Button Grid.Row="1" Margin="300,0,0,0" Click="Next_Click" >Next</Button>
		<Button Grid.Row="1" Margin="230,0,0,0" Click="Prev_Click" >Prev</Button>

	</Grid>
</Page>
